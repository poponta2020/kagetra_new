---
status: draft            # draft | iterating | locked
slug: <slug>
target: <ルート or コンポーネントパス>
chosen_direction: null   # 確定した方向性（例 "A"）。確定まで null
round: 0                 # 調整ループの回数
---
# <画面名> デザイン仕様（design-spec）

> `/design-screen` が出力する確定仕様。実装は `/define-feature <slug>` または `/implement <slug>` で行う。
> このファイルは**見た目の確定形**を記述する。本番コードのファイル割りやテストは実装スキル側で詰める。

## 1. 対象と狙い
- **対象画面:** <ルート / コンポーネント>
- **現状の不満:** <見づらい点・動機>
- **狙い（ゴール）:** <この画面で達成したいこと>
- **主ユーザー / 主な使い方:** <眺める / 下調べ / 自分の確認 等>

## 2. 採用した方向性
- **方向性:** <A/B/C 等＋一言要約>
- **不採用案と理由:** <なぜ他案を採らなかったか（後日の蒸し返し防止）>
- **確定モック:** Claude Design `<画面名> (Redesign)` グループ / `preview/<slug>-<dir>.html`

## 3. レイアウト構成（上→下）
<セクションを上から順に。各セクションの中身・情報の優先順位・余白感>
1. …
2. …

## 4. 使用コンポーネント
- **既存プリミティブ:** Card / Pill / GradePill / Btn / … （`apps/web/src/components/ui/`）
- **新規が必要なもの:** <あれば。折りたたみ行・集計バー・年度ヘッダ 等。挙動と見た目>

## 5. 状態（state）
- **通常:** …
- **空（データ無し）:** …
- **長大（多数の項目）:** <畳み方・初期表示件数・もっと見る 等>
- **エラー / 不明値:** <開催日不明・順位なし 等の扱い>

## 6. 必要データ
- **表示フィールド:** <フィールド名と出所>
- **集計・導出:** <勝率・年度別・優勝/入賞回数などの算出方法と元データ。データ品質の注意（例: 順位は自由記述）>

## 7. インタラクション / レスポンシブ
- **操作:** <折りたたみ展開・フィルタ・タップ遷移 等>
- **モバイル:** 375px 基準。<横スクロール回避・タップ領域 等>

## 8. ガードレール準拠メモ
- 配色（washi×藍墨・二値セマンティック）/ フォント（serif=名前・数字）/ 絵文字無し / 日付・時刻表記 — 逸脱があれば理由を明記。

## 9. 残課題・実装への申し送り
- <未確定点・実装時に判断が要る点・パフォーマンス懸念 等>

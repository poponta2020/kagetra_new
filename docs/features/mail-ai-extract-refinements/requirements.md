---
status: completed
design_required: false
mode: change
base_feature: mail-tournament-import
---

# mail-ai-extract-refinements 要件定義書（改修）

> **改修対象**: [mail-tournament-import](../mail-tournament-import/requirements.md)（§3.2.2 AI 抽出ルール / §3.1.1 受信箱一覧）、
> [tournament-title-grade-split](../tournament-title-grade-split/requirements.md)（§4.1 ExtractionPayloadSchema）、
> [mail-inbox-mailer](../mail-inbox-mailer/requirements.md)（`triggerExtractDraft` / AI 抽出確認ダイアログ）。
> 本書が**変更後の挙動の唯一の正**であり、上記3書の当該箇所は本書で上書きされる。

---

## 1. 概要

### 目的
運用変更（AI の役割が「分類＋抽出」から「抽出のみ」へ縮小）に、プロンプト・スキーマ・UI を追従させる。あわせて抽出項目を実運用で必要なものへ絞り込み、モデルを Claude Sonnet 5 へ移行し、管理者が **どの添付を AI に読ませるかを選べる** ようにする。

### 背景・動機

**運用は既に変わっており、コードの一部だけが取り残されている。**

- かつては cron が新着メールを自動で AI にかけ、AI が「大会案内かどうか」を判定してから抽出していた
- 現在は [index.ts:122-131](../../../apps/mail-worker/src/index.ts) のとおり **cron は AI を呼ばない**（`--mode=fetch` は `llmExtractor: undefined`）。AI が走るのは管理者が受信箱で「会で流す（AI 抽出）」を押した経路（`--mode=extract`）だけ
- つまり **AI に届く時点で「これは大会案内だ」という人間の判断が済んでいる**。にもかかわらずシステムプロンプトの約3割が分類のための記述に費やされ、出力スキーマも `is_tournament_announcement` を要求し続けている
- 抽出項目にも、運用で使われていないもの・他の手段で導出できるものが残っている（通称・参加費・訂正版判定・原文メモ3種）
- 添付は**全 PDF が無条件に AI へ送られる**。会場地図・申込書 Excel・別大会の要綱まで一緒に送られ、トークン代と誤抽出の両方を悪化させている
- 抽出モデルは Sonnet 4.6 のまま。Sonnet 5 は定価同一（2026-08-31 まで導入価格で3割安）で上位互換

### 変更の骨子（6点）

| # | 変更 | 主な効果 |
|---|---|---|
| 1 | モデルを `claude-sonnet-5` へ移行し `thinking: disabled` を明示 | 上位互換・導入価格中は割安 |
| 2 | 分類ロジックをプロンプト・スキーマから撤去 | プロンプト約3割減、誤抽出の余地を削減 |
| 3 | **抽出項目を実運用に合わせて整理**（5項目削除・2項目追加・4項目の意味変更） | 使わない値を取りに行かせない・null の曖昧さを解消 |
| 4 | 添付を選択して AI に渡す UI を新設 | 無関係添付の除外、トークン代削減 |
| 5 | `MAIL_WORKER_PDF_SIZE_LIMIT_KB` を 800 → 8000 へ引き上げ | 実サイズの要綱 PDF が通る |
| 6 | 「本文は常に AI へ渡る」を回帰 AC として固定 | 本文にしかない情報の欠落を防ぐ |

### 2026-09-08 改修: D・E 級の地域制限と北海道の出場可否を AI に判定させる

**背景**: D 級・E 級の大会案内には「近畿支部の会に所属、または在住在学在勤の方」「中国・四国地方に在住・通学・勤務している者」のような**出場地域の制限**が付いていることが多い。ローカル DB にある D・E 級を含む要綱 9 件のうち 8 件に地域制限があり、うち北海道の選手が出場できるのは北海道初心者大会の 1 件だけだった。制限は級ごとに異なる（兵庫: D=近畿＋隣接県、E=兵庫県内）。現状の AI 抽出はこの資格文を `extras.eligible_grades_raw` に生テキストとして残すだけで、管理者が毎回原文を読んで判断している。見落とすと、出場できない大会が会員に案内され、申込グループや級別 LINE 配信まで作られてしまう。

**変更の骨子**:

| # | 変更 | 主な効果 |
|---|---|---|
| 7 | 各単位の **D 級・E 級**について、地域制限の有無と北海道の選手の出場可否を **4 値**で判定させる（§3.2.12） | 原文を読み直さなくても対象外の級が分かる |
| 8 | 判定の**根拠となる一文を資料から一字一句そのまま**抜き出させ、ワーカーが**原文と機械照合**する | AI の要約・言い換え・捏造を自動除外の根拠にしない |
| 9 | 承認フォームで、照合済みの「北海道は対象外」の級を**対象級の初期値から外し、外したことを警告表示**する | 管理者の手数を減らしつつ、AI の判断を黙って通さない |

---

## 2. ユーザーストーリー（差分）

対象ユーザーは従来どおり **管理者（`admin` / `vice_admin`）実質1名**。変わるのは以下のシナリオ。

**シナリオ A': 大会案内を取り込む（変更後）**
1. 管理者が `/admin/mail-inbox` でメールを開き、これは大会案内だと判断する
2. 「会で流す（AI 抽出）」を押す → **添付一覧が出る。既定では全て未チェック**
3. 要綱 PDF にチェックを入れる（会場地図や申込書 Excel は入れない）
4. 「はい」で AI 抽出が走る。**本文は選択によらず必ず AI に渡る**
5. 抽出完了後、ドラフト詳細で内容を確認する。**通称欄に「大阪」と入力**すると、各単位の大会名が「大阪B」「大阪C」と自動合成される
6. 承認して events を作成

**シナリオ B': 渡す資料を間違えた**
1. 管理者が要綱ではなく名簿 PDF にチェックを入れて実行してしまう
2. AI は `source_mismatch: true` を返し、`reason` に「渡された PDF は出場者名簿に見える」と書く
3. ドラフトは通常どおり `pending_review` で作られるが、詳細画面に警告が出る
4. 管理者は「再 AI 抽出」から**添付を選び直して**やり直す

**シナリオ C': 大きな要綱 PDF**
1. 要綱 PDF が 12MB あり、注意の目安（8MB）を超えている
2. 選択ダイアログでその添付は**チェックでき**、行に「サイズが大きめです」と表示される
3. 実行すると「サイズの大きい添付があります」の確認が1段入り、**続行すればそのまま AI に渡る**
4. 選択した PDF の合計が Anthropic の 32MB 予算（生バイトで約 23MB）を超える場合だけは送れないので、添付を減らす

**シナリオ D': 訂正版が届いた（AI は関与しない）**
1. 訂正版メールが届く。管理者が本文を読んで訂正版だと気づく
2. 該当する既存 events を**手動で**編集する。AI に訂正版判定はさせない

**シナリオ E': D・E 級に地域制限がある案内（2026-09-08 追加）**
1. 兵庫大会（A〜E 級同日）の要綱を AI 抽出する
2. AI は D 級を「北海道は対象外」（根拠: 「D級 初段の方 地域制限有 近畿支部及び隣接県（鳥取、岡山、徳島）の会所属、及び支部内在住・在勤・在学の方」）、E 級を「北海道は対象外」（根拠: 「E級 初段を目指す方。 地域制限有 兵庫県内の会所属、及び兵庫県内在住・在勤・在学の方」）と返す。A〜C 級は判定対象外
3. ワーカーが根拠の一文を本文・添付の抽出テキストと照合し、一致したので「照合済み」として保存する
4. 承認フォームを開くと、その単位の対象級は **A・B・C だけがチェック済み**で、大会名は「兵庫ABC」と合成されている。単位の上に「D級・E級を対象級から外しました」の警告と根拠の一文が出る
5. 管理者は根拠を一読し、そのまま承認する。外すべきでないと判断すれば、対象級のチェックを戻すだけでよい

**シナリオ F': 全ての級が対象外（2026-09-08 追加）**
1. 丸亀大会（D・E 級のみ）の要綱を AI 抽出する。D 級「中国・四国地方に在住・通学・勤務している者」、E 級「四国地方、岡山県に在住・通学・勤務している者」でどちらも北海道は対象外
2. 承認フォームではその単位の「このイベントを登録する」が**既定で OFF** になり、「この日の全ての級が北海道の選手は出場できないため、登録対象から外しました」と警告が出る
3. 他に登録する単位が無ければ、管理者はドラフトを却下する

**シナリオ G': 根拠を照合できない・条件付き（2026-09-08 追加）**
1. 埼玉県大会 D 級は「埼玉県かるた協会の個人会員又は団体会員…に所属すること」が原則だが、申込が少なければ県内在住者へ緩和すると書かれている。AI は「要確認」を返す
2. または、要綱が画像だけの PDF で抽出テキストが無く、AI の根拠の一文を原文と照合できなかった
3. どちらの場合も**対象級は外さない**。単位の上に「D 級の出場資格を確認してください」と根拠の一文（照合できなかった場合はその旨）が出る。判断は管理者が行う

---

## 3. 機能要件

### 3.1 画面と遷移

新規ルートは増えない。ダイアログはいずれも既存画面内のモーダル。

| 画面 | 変更内容 |
|---|---|
| `/admin/mail-inbox`（一覧） | ①**tier 分けを廃止**（`ConfidenceBadge` 削除、`confidence >= 0.9` による「要対応 / 要確認」の2段振り分けをやめ、`pending_review` を受信日降順の一本に）②カードの表示名を **`formal_name`** に変更（`composeTitle(stem, grades)` は AI が stem を出さなくなるため使えない。`formal_name` も無い単位は「開催日＋級」で代替） |
| `/admin/mail-inbox/mail/[id]`（メール詳細） | 「会で流す（AI 抽出）」ダイアログを**添付選択ダイアログに拡張**（3.2.4） |
| `/admin/mail-inbox/[id]`（ドラフト詳細） | ①`ConfidenceBadge` 削除 ②**通称入力欄を新設**（3.2.3）③`source_mismatch: true` のとき警告バナーを表示 ④**訂正版ヒント（関連ドラフト検索）を撤去** ⑤「再 AI 抽出」から添付選択ダイアログを再度開けるようにする |
| `/admin/mail-inbox/[id]`（ドラフト詳細・2026-09-08 追加） | ⑥ 承認フォームの各単位に**地域制限の警告ブロック**を追加（照合済みの「北海道は対象外」の級を対象級の初期値から外した旨と根拠の一文／「要確認」「未照合」の級への確認依頼／「北海道は対象」の補足）⑦ 全ての級が外れた単位は「このイベントを登録する」を既定 OFF ⑧ AI 抽出結果ビュー（`ExtractedPayloadView`）に級ごとの判定・根拠・照合状態を表示 |

### 3.2 ビジネスルール

#### 3.2.1 AI 抽出ルール（mail-tournament-import §3.2.2 を置き換え）

- **モデル**: `claude-sonnet-5`（プロバイダ抽象化レイヤ経由は従来どおり）
- **`thinking`**: `{ type: 'disabled' }` を**明示的に指定する**。Sonnet 5 は省略時 adaptive thinking が ON になり、`max_tokens` が思考と出力を合算して上限をかけるため、明示しないと `record_extraction` の引数が途中で切れる
- **プロンプトキャッシュ**: **撤去する**。手動起動＝メール1件ごとの散発的な呼び出しになったため、どの TTL でもキャッシュヒットせず書込プレミアム（1h で 2.0×）を払うだけになる
- **structured output**: 従来どおり forced tool use（`record_extraction`）
- **入力**: メール本文（**選択内容によらず常に渡す**）＋ **管理者が選択した添付だけ**。PDF はネイティブ document ブロック、DOCX 等は抽出テキスト。XLSX は従来どおり抽出無効
- **分類は行わない**。渡された資料が大会要綱である前提で抽出に専念する
- **`PROMPT_VERSION` は `3.0.0`**（出力スキーマの破壊的変更＝major）

#### 3.2.2 出力スキーマ（変更後の全項目）

**案内全体に1つ持つ項目**

| フィールド | 型 | 意味 | 変更 |
|---|---|---|---|
| `events` | `EventUnit[]` | 開催日ごとに1要素。**1件以上必須** | 維持 |
| `reason` | `string` | **レビュー画面向けの抽出メモ**（「級別の定員が読み取れなかった」「開催日が期間表記のため null」等）。`source_mismatch: true` のときはその理由を書く | 意味変更 |
| `source_mismatch` | `boolean \| null` | 渡された資料が明らかに別種の文書に見える場合に `true` | **追加** |
| `extras` | object（任意） | `eligible_grades_raw`（出場資格の原文）/ `target_grades_raw`（対象階級の原文）のみ | 3項目削除 |

**削除する全体項目**

| フィールド | 削除理由 |
|---|---|
| `is_tournament_announcement` | 分類は人間が済ませている |
| `confidence` | 「分類が正しい確率」として定義されており意味を失う。校正不能な自己申告スコアを別定義で残さない |
| `short_name_stem` | 通称は承認フォームで人間が入力する（3.2.3） |
| `is_correction` / `references_subject` | 訂正版はほぼ届かず、届いても軽微。人力確認で運用可能 |
| `extras.fee_raw_text` | `fee_jpy` を廃止するため不要 |
| `extras.local_rules_summary` / `extras.timetable_summary` | 運用で参照していない |

> **`source_mismatch` は分類機能の復活ではない。** プロンプトに「これは大会案内か判定せよ」と書いてはならない。指示するのは「**渡された資料が明らかに別種の文書だったときだけ申告せよ**」という一点に限る。前者を書くと、今回撤去した判断をそのまま別名で呼び戻すことになる。

**開催日ごとに1組持つ項目（`EventUnit`・21項目）**

分割ルールは従来どおり。同日複数級は1単位にまとめ、級で開催日が違えば単位を分ける。

| フィールド | 型 | 意味 | 変更 |
|---|---|---|---|
| `unit_key` | `string` | `"u1"` `"u2"`…（`^u[1-9]\d*$`）。承認フォームのフィールド名前空間に使うため一意必須 | 維持 |
| `event_date` | `"YYYY-MM-DD" \| null` | 開催日（JST）。和暦→西暦・全角→半角。**期間表記は `null`** | 維持 |
| `eligible_grades` | `("A".."E")[] \| null` | その日に行われる級 | 維持 |
| `formal_name` | `string \| null` | 正式名称（「第5回大阪大会B級」）。**edition 名寄せの種であり一覧カードの表示名でもある** | 維持（重要度上昇） |
| `venue` | `string \| null` | 会場名＋住所 | 維持 |
| `entry_deadline` | `"YYYY-MM-DD" \| null` | 申込締切。期間表記は**終了日**を採用 | 維持 |
| `payment_deadline` | `"YYYY-MM-DD" \| null` | 振込締切。`payment_deadline_kind` が「日付あり」のときだけ値が入る | 維持 |
| `payment_deadline_kind` | `"日付あり" \| "後日連絡" \| "記載なし"` | 振込締切の**状態**。案内に「振込先は抽選後に別途連絡」等があれば「後日連絡」。**承認時に events へも持ち回す**（3.2.7） | **追加** |
| `payment_info_text` | `string \| null` | 振込先の生テキスト（「ゆうちょ 12340-12345-1 …」） | 維持 |
| `payment_method` | `"口座振込" \| "現地支払い" \| "その他" \| null` | 支払い方法 | **enum 化・日本語化** |
| `entry_method` | `"Excel申込書" \| "Googleフォーム" \| "メール" \| "その他" \| null` | 申込方法 | **enum 化・日本語化** |
| `organizer_text` | `string \| null` | 主催団体 | 維持 |
| `kind` | `"individual" \| "team" \| null` | 個人戦／団体戦。混合は `null` | 維持 |
| `capacity_total` | `int \| null` | **全体定員**（「定員100名」）。明示があるときだけ | **追加** |
| `capacity_a` 〜 `capacity_e` | `int \| null` | 級別定員。明示があるときだけ | 維持 |
| `official` | `boolean \| null` | 公認大会（協会公認・連盟主催の段位戦）なら `true` | 維持 |
| `regional_eligibility` | `RegionalEligibility[]` | **D 級・E 級の地域制限判定**（§3.2.12）。要素は `{ grade: "D" \| "E", verdict: 4値, evidence_quote: string \| null }`。その単位の `eligible_grades` に含まれる D・E だけを対象に 1 級 1 要素。D・E を含まない単位は空配列 | **追加（2026-09-08）** |

**削除する単位項目**

| フィールド | 削除理由 |
|---|---|
| `fee_jpy` | 級から導出可能（A/B 2,500・C/D 2,000・E 1,500。公認料は全級 300）。`packages/shared` に定数として実装済み |

**定員の扱い**: 全体定員と級別定員が両方書かれていれば**両方抽出する**。片方からの逆算・均等割りは従来どおり**禁止**（推定値を入れさせない）。

**バリデーション**: `superRefine` のノイズ2分岐（`true` かつ空配列 / `false` かつ非空配列）を削除し、**無条件に `events.length >= 1` を要求**する。`unit_key` の一意性チェックは維持する。`payment_deadline_kind` が「日付あり」なのに `payment_deadline` が `null` の組み合わせは矛盾として弾く。

#### 3.2.3 大会名の決定方法（`short_name_stem` 廃止に伴う変更）

- 承認フォーム上部に**「通称」欄を1つ新設**する。管理者が「大阪」「札幌」のような地名だけを入力する
- 各単位の大会名は `composeTitle(通称, eligible_grades)` で**自動合成**する（「大阪」＋`["B"]` → 「大阪B」）。合成ロジック自体は現行の `composeTitle()` をそのまま使い、**stem の供給元が AI から人間に変わるだけ**
- 単位ごとに合成結果を**個別に上書きできる**（現行フォームの挙動を維持）
- 通称が未入力のあいだ、合成結果は空にする（級だけの「B」のような無意味な値を出さない）
- 通称の**供給**（初期値・候補提示）と系列選択の連動は 3.2.9 に定める。人が最終的に決める点は変わらない

#### 3.2.9 通称と系列の相互連動（2026-09-07 追加）

会が使う「通称」は、系列マスタ `tournament_series.short_name`（大阪／多摩／北國…）と**同じ概念**である。にもかかわらず現状は、通称を人が打ち、同じ語をもう一度系列検索シートに打ち込んでいる。両者を連動させ、片方の入力だけで両方が決まるようにする。

**(a) フォームを開いた時点の初期値（自動投入）**

- AI 抽出の代表 `formal_name` から作った系列名候補を既存系列へ名寄せし、**候補が1件だけ**のとき、その系列を選択済みにしたうえで、その系列の `short_name` を**通称欄の初期値**として入れる
  - この緩和は既存の「**正規化完全一致が単独なら採用**」を**置き換えるのではなく足す**。完全一致が単独なら、他に部分一致の候補があっても従来どおり採用する（一意な完全一致は曖昧ではない。置き換えにすると、現に効いている自動化を落とすことになる）
  - これは [tournament-entry-rosters](../tournament-entry-rosters/requirements.md) §3.1・AC-1/AC-2 の「**正規化完全一致が1件のときだけ**初期選択」を**緩和して上書き**する（部分一致でも候補が1件に絞れているなら初期選択する）。実測31件では完全一致はごく少数で、大半が部分一致1件に落ちており、旧条件では自動化がほぼ効かない
- 候補が0件または複数のときは、通称欄は空・系列は未選択のままにする（現行どおり。曖昧なものを勝手に確定しない）
- 候補1件でもその系列の `short_name` が `null` のときは、**系列だけを選択し通称欄は空**にする
- 自動投入したときは、通称欄の近くに**どの系列から入れたのかが分かる表示**を出す（人が気づかないまま誤った大会名で登録されるのを防ぐ）

**(b) 通称 → 系列（候補チップ）**

- 通称欄に文字を入力すると、その文字列で既存系列を検索し、**通称欄の直下に候補チップ**を出す。チップは「通称（系列の正準名）」の形で示し、`short_name` が無い系列は正準名だけを示す
- チップをタップすると**その系列が確定する**。このとき**通称欄の文字列は書き換えない**（人が意図して打った表記を残す。会の表記「椿杯」と DB の `short_name`「椿」のようなずれは実在する）
- チップは常に入力へ追従する。**通称を打ち直しても、確定済みの系列選択は外れない**（別系列にしたければチップか検索シートで選び直す）
- 個人戦／団体戦が混在する案内では、現行どおり候補を出さない

**(c) 系列 → 通称**

- 系列検索シートで系列を選んだとき、**通称欄が空ならその系列の `short_name` を入れる**。既に何か入力されていれば触らない

**(d) 系列マスタを育てる**

- 承認フォームから**新しい系列を作成するとき、通称欄の値を `short_name` として保存**する。通称欄が空なら `short_name` は `null` のままとする
- 既存系列の `short_name` を承認フォームから書き換えることはしない（上書き事故を避ける）

#### 3.2.10 系列検索の照合対象に通称を加える（2026-09-07 追加）

- 管理者の検索（検索シート・候補チップ）の照合対象を、現行の「正準名＋別名」に**`short_name` を加えた3種**にする。完全一致は先頭に来るため、「大阪」と打ったときに本命の「大阪大会」が候補の先頭に立つ（現行は五十音順で「初段認定大阪なにはえ会大会」等に埋もれる）
- **自動解決側（`scoreSeries` / `rankSeriesCandidates` = 案内承認の初期候補・結果取込 flow② が使う双方向包含スコア）は変更しない**。検索は一方向・自動解決は保守的、という既存の分離を保つ

#### 3.2.11 回次の漢数字対応（2026-09-07 追加）

- 大会名の回次パースが**漢数字の「第三回」を 3 として読める**ようにする（現行は算用数字・全角数字のみ。実例: 第三回全国競技かるた杉並大会が回次不明となり系列に紐付けられなかった）
- 回次が読めない大会（例「九段大会2026-2」のような年式表記）は従来どおり紐付け対象外とする

#### 3.2.12 D・E 級の地域制限と北海道の出場可否（2026-09-08 追加）

**(a) 判定対象**

- 各単位の `eligible_grades` に含まれる **D 級・E 級だけ**を判定する。A〜C 級は判定しない（C 級にも地域制限の実例はあるが、今回の対象外。§5）
- D・E を含まない単位、`eligible_grades` が `null` の単位では `regional_eligibility` は空配列
- 「北海道の選手」とは、**北海道に在住・在勤・在学する、または北海道の会に所属する選手**を指す。会員個人の事情（道外在住の会員・登録会が他会のゲスト）は見ない（§5）

**(b) 4 値の判定（`verdict`）**

| 値 | 意味 | 例 |
|---|---|---|
| 制限なし | 出場資格に地域の条件が無い | 「E 級（無段） 百首の決まり字がわかり、競技ルールを理解し、自力で試合ができる選手」／横浜 E 級「参加者の地域制限は設けませんが」 |
| 北海道は対象 | 地域の条件があり、北海道が含まれる | 北海道初心者大会「道内に在住，通学，通勤する無段者」 |
| 北海道は対象外 | 地域の条件があり、北海道が含まれない | 「近畿支部（二府四県）のかるた会に所属、または在住在学在勤の方」「北陸支部内及び隣接県（新潟県、長野県、岐阜県）に在住・在勤・在学者に限る」 |
| 要確認 | 条件付き・緩和条項あり・地域を特定できない・資格の記載が見当たらない | 埼玉「申込数が上限数に対して大幅に少なかった場合は…埼玉県在住、在勤又は在学である者の参加を認めます」 |

- **抽選の優先枠（主催者枠・地元枠）は地域制限ではない**。「地域制限は設けないが抽選時は県内を優先」は「制限なし」とする
- **海外選手の特例**（「海外選手はこの限りではない」）は判定に影響させない
- **所属会を条件にする書き方**（「○○県かるた協会加盟の会に所属」「福岡県協傘下登録会所属」）は、北海道の会がそれを満たさないので「北海道は対象外」
- 支部の地理は AI の知識に任せる（北海道は北海道支部であり、東北・関東・北陸・東海・近畿・中国・四国・九州のいずれにも属さない）
- D・E で条件が異なる案内は級ごとに別々に判定する（丸亀: D=中国・四国、E=四国＋岡山）
- 資格の記載が見当たらないときは「制限なし」と推測せず「要確認」にする（D・E 級の案内は地域制限がある方が多数派であり、無言の「制限なし」は見落としと区別できない）

**(c) 根拠の一文（`evidence_quote`）**

- 判定の根拠にした箇所を、**資料から一字一句そのまま**抜き出す。要約・言い換え・整形（全角半角の変換、空白や改行の除去、句読点の補正、「…」による省略）を**しない**。長さは一文、必要なら連続する数文まで
- 「制限なし」「北海道は対象」「北海道は対象外」では **必須**（`null`・空文字は不可）。「要確認」だけは根拠が無い（記載が見当たらない）場合に `null` を許す
- **ワーカーが機械照合する**: AI に渡した入力（本文＋選択した添付の抽出テキスト）と根拠の一文の双方から空白類を除去し Unicode NFKC 正規化したうえで、根拠が入力の部分文字列であれば「照合済み」、そうでなければ「未照合」として、判定と一緒にドラフトへ保存する。空白だけを無視するのは、PDF の抽出テキストに「202 6 年」のような抽出由来の空白が混じるため
- 抽出テキストを持たない添付（画像だけの PDF）しか根拠を含まない場合は、必然的に「未照合」になる。これは異常ではなく、自動で外さず人が見る状態へ倒すための設計
- 照合結果は判定を書き換えない（AI が「北海道は対象外」と言った事実は残す）。**自動で外すかどうかだけが照合結果に従う**（(d)）

**(d) 承認フォームでの扱い**

| 判定 | 照合 | 対象級の初期値 | 表示 |
|---|---|---|---|
| 北海道は対象外 | 照合済み | **外す**（`composeTitle` の入力にも反映。兵庫 A〜E → 「兵庫ABC」） | 警告「D級・E級を対象級から外しました（北海道の選手は出場できないため）」＋級ごとの根拠の一文＋「戻すには下の対象級を再チェック」 |
| 北海道は対象外 | 未照合 | 外さない | 警告「D級は北海道は対象外と判定されましたが、根拠の一文を原文と照合できませんでした。確認してください」＋根拠の一文 |
| 要確認 | 問わず | 外さない | 注意「D級の出場資格を確認してください」＋根拠の一文（無ければ「資格の記載が見当たりません」） |
| 北海道は対象 | 問わず | 外さない | 補足「D級は地域制限ありですが北海道は対象です」＋根拠の一文 |
| 制限なし | 問わず | 外さない | フォームには出さない（AI 抽出結果ビューで見られる） |

- 外すのは**フォームの初期値だけ**。管理者は対象級のチェックを戻せる。承認 Server Action は送信された対象級をそのまま保存し、判定を再適用しない（人間の判断が AI より上位。§3.2.6 と同じ原則）
- **単位の全ての級が外れた**とき（D・E 級のみの案内で両方が対象外）、その単位の「このイベントを登録する」を**既定 OFF** にし、「この日の全ての級が北海道の選手は出場できないため、登録対象から外しました。他に登録する日が無ければ却下してください」と表示する。ON に戻せば通常どおり登録できる
- 既に登録済み（materialize 済み）の単位には何もしない
- `regional_eligibility` を持たないドラフト（PROMPT_VERSION 3.0.x 以前）では何も外さず、警告も出さない
- 承認をブロックしない。新しいステータスも作らない

**(e) AI 抽出結果ビュー**

- `ExtractedPayloadView` に級ごとの判定・根拠の一文・照合状態を表示する（判定はフォームに出ない「制限なし」も含めて全件）

**(f) プロンプトと版数**

- システムプロンプトに (a)〜(c) の指示と、D・E 級で条件が異なる案内の few-shot 例を加える。既存の few-shot 例には `regional_eligibility` を追記する
- `PROMPT_VERSION` は **`3.1.0`**（追加のみ＝minor）

#### 3.2.4 添付選択ルール（新規）

- 選択ダイアログは**「会で流す（AI 抽出）」と「再 AI 抽出」の両方**で使う
- 各添付について**ファイル名・種別・サイズ**を表示する
- **既定は全て未チェック**
- **`MAIL_WORKER_PDF_SIZE_LIMIT_KB` を超える添付も選択できる**。行に「サイズが大きめです」の注意書きを出し、実行時に確認を1段挟む（続行すればそのまま AI に渡る）
- **添付が1つ以上あるのに全て未チェックのまま実行**しようとした場合、「本文だけで実行しますか？」の確認を1段挟む
- **添付が0件のメール**では確認を挟まず実行できる
- 選択内容は永続化し、**「再 AI 抽出」時に前回の選択を初期値として復元**する。そのうえで選び直せる。復元で落とすのは**添付そのものが削除された id だけ**で、サイズを理由には落とさない
- Server Action は UI を経由しない不正な選択（当該メールに属さない添付 ID・合計サイズが 32MB 予算を超える選択）を**サーバー側で拒否する**

#### 3.2.5 サイズのしきい値とスキップ挙動

- `MAIL_WORKER_PDF_SIZE_LIMIT_KB` の既定値は **8000**（800 から引き上げ）。役割は**注意のしきい値**であり、送信可否は決めない
- 送信を止めるのは Anthropic の **32MB リクエスト上限**に由来する合計ガードだけ。事前判定は base64 化後のサイズで行い、非添付ぶんの予約枠 1MiB を引いた 31MiB を枠とする（生バイトで約 23.25MB）
- classifier 側の `oversize_skipped` ガードは**合計サイズと送信直前の実測**に対して残す。1件ごとのサイズでは `oversize_skipped` にしない

#### 3.2.6 `source_mismatch` の扱い

- ドラフトは通常どおり `pending_review` で作成する。新しいステータスは作らない
- ドラフト詳細画面に警告バナーを表示し、`reason` の内容を併記する
- **承認をブロックしない**。人間の判断が AI より上位である

#### 3.2.7 振込締切の状態を events へ持ち回す

**動機**: 支払いを追いかけるのはドラフトを見た人ではなく、後から申込管理ボードを見る人である。状態をドラフト payload の中だけに持つと、承認した瞬間に「後日連絡だから空」という情報が消え、[entry-board-utils.ts:624](../../../apps/web/src/app/(app)/admin/entries/entry-board-utils.ts) が一律「締切未設定」と表示する現状に戻ってしまう。

- `events.payment_deadline_kind` を追加する。値は `fixed` / `later_notice` / `unspecified`（**DB は英語値**。既存の `eventPaymentTypeEnum` が `advance` / `onsite` である慣行に合わせる。UI は日本語で表示する）
- 承認時に payload の日本語値をマッピングする（「日付あり」→`fixed` /「後日連絡」→`later_notice` /「記載なし」→`unspecified`）
- **`payment_deadline` の有無と `payment_deadline_kind` を CHECK 制約で双条件に縛る**: `(payment_deadline IS NOT NULL) = (payment_deadline_kind = 'fixed')`
- 既存行の backfill: `payment_deadline IS NOT NULL` → `fixed`、それ以外 → `unspecified`（この規則で全既存行が CHECK を満たす）
- 編集フォームで**日付を入力したらサーバー側で `fixed` に正規化**する（人間が「後日連絡」のまま日付を入れても矛盾状態にならない）

**表示する3面**

| 画面 | 変更内容 |
|---|---|
| `/admin/entries`（申込管理ボード） | `payment_deadline` が `null` のとき、`later_notice` なら「**後日連絡**」、`unspecified` なら従来どおり「締切未設定」と出し分ける |
| `/events/[id]`（イベント詳細） | 支払い関連の表示に状態を反映する |
| `/events/[id]/edit`（イベント編集） | 状態を手動変更できる。**後日連絡だったものに連絡が来たとき、人間が日付へ書き換えられる**ことがこの機能の実用上の要 |

#### 3.2.8 一覧の優先度分け廃止

- `confidence >= 0.9` → tier0「要対応」／`< 0.9` or `null` → tier1「要確認」の振り分けを**廃止**する
- `pending_review` のドラフトを受信日降順で一本に並べる
- `ConfidenceBadge` コンポーネントを削除する
- `tournament_drafts.confidence` **列は DROP しない**。書き込みを止め、表示をやめるだけにする

---

## 4. Acceptance Criteria

| ID | 条件 | 検証手段 |
|----|------|------|
| **プロンプト・スキーマ（分類撤去）** | | |
| AC-1 | `record_extraction` の input_schema に `is_tournament_announcement` / `confidence` / `short_name_stem` / `is_correction` / `references_subject` / `fee_jpy` が存在しない | auto-test |
| AC-2 | `extras` に `fee_raw_text` / `local_rules_summary` / `timetable_summary` が存在しない | auto-test |
| AC-3 | `events` が空配列のペイロードは Zod validate に失敗し、classifier の 1 回リトライ経路に入る | auto-test |
| AC-4 | `unit_key` 重複のペイロードは従来どおり validate に失敗する | auto-test |
| AC-5 | システムプロンプトに「大会案内でない」「confidence」「メーリングリストのダイジェスト」「訂正」の文字列が含まれない | auto-test |
| AC-6 | `PROMPT_VERSION` が `3.0.0` で、生成される `tournament_drafts` 行に記録される | auto-test |
| AC-7 | `source_mismatch: true` のドラフトは `pending_review` になり、詳細画面に警告バナーが表示され、**承認ボタンは押せる**（ブロックされない） | auto-test |
| **新規・変更項目** | | |
| AC-8 | `payment_deadline_kind` が「日付あり」「後日連絡」「記載なし」の3値のみを取る。「日付あり」なのに `payment_deadline` が `null` の組み合わせは validate に失敗する | auto-test |
| AC-9 | 案内本文に「振込先は抽選後に別途ご連絡します」がある fixture で `payment_deadline_kind: "後日連絡"` が返る | auto-test |
| AC-10 | `payment_method` が「口座振込」「現地支払い」「その他」`null` 以外の値を取らない | auto-test |
| AC-11 | `entry_method` が「Excel申込書」「Googleフォーム」「メール」「その他」`null` 以外の値を取らない | auto-test |
| AC-12 | 「定員100名」のように全体定員のみが書かれた fixture で `capacity_total` に値が入り、`capacity_a`〜`e` は `null` のままである | auto-test |
| AC-13 | 全体定員と級別定員が併記された fixture で**両方**が抽出される | auto-test |
| AC-14 | 全体定員から級別定員を按分した推定値が入らない（級別の明示がない fixture で `capacity_a`〜`e` が全て `null`） | auto-test |
| **大会名（通称の人力入力）** | | |
| AC-15 | 承認フォームに通称欄があり、「大阪」を入力すると各単位の大会名が `composeTitle` で「大阪B」「大阪C」と合成される | auto-test |
| AC-16 | 合成された大会名を単位ごとに個別上書きできる | auto-test |
| AC-17 | 通称が未入力のあいだ、合成結果が空になる（級だけの値が出ない） | auto-test |
| AC-18 | 一覧カードの表示名が `formal_name` になり、`formal_name` が無い単位は「開催日＋級」で表示される | auto-test |
| AC-19 | ドラフト詳細から訂正版ヒント（関連ドラフト検索）が消える | auto-test |
| **回帰（抽出精度）** | | |
| AC-20 | **`messages.create` に渡るリクエストボディ**に `mail.bodyText` を含む text ブロックが必ず存在する。次の4ケースで検証する: ①添付ありのメールで添付ゼロ選択 ②添付0件のメール ③text 種別の添付のみを選択 ④選択を復元した再 AI 抽出 | auto-test |
| AC-21 | 既存の抽出 fixture において、開催日ごとの `events` 分割・和暦→西暦変換・全角数字の半角化・申込期間の終了日採用・級別定員が従来どおり抽出される。**fixture の移行手順は以下に限定する**（回帰ベースラインを壊さないため、再生成は禁止）:<br>①`event_date` / `eligible_grades` / `formal_name` / `venue` / `entry_deadline` / `payment_deadline` / `payment_info_text` / `organizer_text` / `kind` / `capacity_a`〜`e` / `official` は**既存の値をそのまま**（1バイトも変えない）<br>②`fee_jpy` と削除対象の全体項目を除去<br>③`payment_deadline_kind` を追加（`payment_deadline` に日付があれば「日付あり」、無ければ「記載なし」。元案内が「後日連絡」を含む場合のみ「後日連絡」）<br>④`capacity_total` を追加（元案内に全体定員の記載がなければ `null`）<br>⑤`payment_method` / `entry_method` は既存値を新 enum へ読み替える（`bank_transfer`→「口座振込」等）<br>⑥`newsletter.expected.json` はノイズ陰性ケースのため削除し、`FixtureLLMExtractor` の既定応答の扱いを合わせて見直す | auto-test |
| **モデル移行** | | |
| AC-22 | `messages.create` に `model: 'claude-sonnet-5'` と `thinking: { type: 'disabled' }` が渡る | auto-test |
| AC-23 | system ブロックに `cache_control` が付かない | auto-test |
| AC-24 | 代表的な大会案内 PDF 1件について `claude-sonnet-4-6` / `claude-sonnet-5` の `count_tokens` を実測し、結果を `docs/features/mail-ai-extract-refinements/token-baseline.md` に記録する。**実測でトークン数が 1.5 倍を超えた場合は移行の可否をユーザーに再確認する** | manual |
| AC-25 | 実際の抽出 1 件で `record_extraction` が `stop_reason: max_tokens` にならず完走する | verify |
| **添付選択 UI** | | |
| AC-26 | AI 抽出ダイアログに添付一覧（ファイル名・種別・サイズ）が表示され、既定で全て未チェックである | auto-test |
| AC-27 | 注意の目安を超える添付も**チェックでき**、注意書きが表示される。実行時にサイズ確認が1段入り、続行すれば AI に渡る | auto-test |
| AC-28 | 添付が1つ以上あるのに全て未チェックで実行しようとすると確認が1段入る | auto-test |
| AC-29 | 添付が0件のメールでは確認なしで実行できる | auto-test |
| AC-30 | 選択した添付だけが AI に渡る（未選択の PDF が document ブロックに含まれない） | auto-test |
| AC-31 | 選択内容が永続化され、「再 AI 抽出」時に前回の選択が初期値として復元される。そのうえで選び直せる | auto-test |
| AC-32 | Server Action が当該メールに属さない添付 ID・合計サイズが 32MB 予算を超える選択を拒否する。1件ごとの目安サイズでは拒否しない | auto-test |
| **一覧・サイズ上限・全体回帰** | | |
| AC-33 | 受信箱一覧から `ConfidenceBadge` と tier 分けが消え、`pending_review` が受信日降順の一本の並びになる | auto-test |
| AC-34 | `confidence` や旧フィールドを持つ既存ドラフトを開いても画面が壊れない（後方互換） | auto-test |
| AC-35 | `MAIL_WORKER_PDF_SIZE_LIMIT_KB` の既定値が `8000` である（役割は注意のしきい値。送信可否は決めない） | auto-test |
| AC-36 | 選択された添付の合計が予算内なら `oversize_skipped` にならない。合計は**選択後の集合**に対して判定し、選択から外れた添付は数えない | auto-test |
| AC-37 | cron（`--mode=fetch`）は従来どおり AI を呼ばない | auto-test |
| AC-38 | 既存テスト・lint・typecheck が CI で green | auto-test |
| **振込締切の状態を events へ持ち回す** | | |
| AC-39 | 承認時に payload の `payment_deadline_kind` が `events.payment_deadline_kind` へマッピングされる（「日付あり」→`fixed` /「後日連絡」→`later_notice` /「記載なし」→`unspecified`） | auto-test |
| AC-40 | CHECK 制約 `(payment_deadline IS NOT NULL) = (payment_deadline_kind = 'fixed')` が効いており、矛盾する組み合わせを INSERT / UPDATE できない | auto-test |
| AC-41 | 既存行の backfill 後、`payment_deadline` を持つ行が `fixed`、持たない行が `unspecified` になり、全行が CHECK を満たす | auto-test |
| AC-42 | 申込管理ボードで `payment_deadline` が `null` のとき、`later_notice` なら「後日連絡」、`unspecified` なら「締切未設定」と出し分けられる | auto-test |
| AC-43 | イベント編集フォームで振込締切の状態を変更でき、**日付を入力するとサーバー側で `fixed` に正規化される**（「後日連絡」のまま日付が入る矛盾状態にならない） | auto-test |
| AC-44 | イベント詳細画面で振込締切の状態が日本語で表示される | auto-test |
| **通称と系列の相互連動（2026-09-07 追加）** | | |
| AC-45 | 正式名称からの名寄せ候補が1件だけのとき、その系列が選択済みになり、通称欄にその系列の `short_name` が入る | auto-test |
| AC-46 | 名寄せ候補が1件でもその系列の `short_name` が `null` のときは、系列だけが選択され通称欄は空のままである | auto-test |
| AC-47 | 曖昧な候補は確定しない。**正規化完全一致が単独のときは（他に部分一致があっても）従来どおり採用する**が、それ以外で名寄せ候補が0件または複数のときは、通称欄が空・系列が未選択のままである | auto-test |
| AC-48 | 通称を自動投入したとき、どの系列から入れたのかが通称欄の近くに表示される | auto-test |
| AC-49 | 通称欄に文字を入力すると、その語で絞り込んだ系列候補チップが通称欄の直下に表示される | auto-test |
| AC-50 | 候補チップをタップすると系列が確定し、**通称欄の文字列は変化しない** | auto-test |
| AC-51 | 通称を打ち直しても確定済みの系列選択は外れず、候補チップだけが新しい入力に追従する | auto-test |
| AC-52 | 個人戦／団体戦が混在する案内では候補チップが表示されない | auto-test |
| AC-53 | 系列検索シートで系列を選んだとき、通称欄が空なら `short_name` が入り、既に入力があれば変化しない | auto-test |
| AC-54 | 系列検索が `short_name` も照合対象にする（「大阪」で「大阪大会」が完全一致として候補の先頭に来る） | auto-test |
| AC-55 | 新しい系列を作成して承認すると、通称欄の値が `tournament_series.short_name` に保存される | auto-test |
| AC-56 | 通称欄が空のまま新しい系列を作成した場合、`short_name` は `null` で作成される | auto-test |
| AC-57 | 既存系列を選んで承認しても、その系列の `short_name` は書き換わらない | auto-test |
| AC-58 | 回次パースが漢数字を読む（「第三回」→ 3、「第二十五回」→ 25）。算用数字・全角数字の既存挙動は変わらない | auto-test |
| **回帰（2026-09-07 追加分）** | | |
| AC-59 | 大会一覧（年別）と選手戦績の通称表示が従来どおりである（同じ `short_name` 列を消費するため） | auto-test |
| AC-60 | 自動解決（`scoreSeries` / `rankSeriesCandidates`）のスコアリングが変わらず、結果取込 flow② の系列解決挙動が従来どおりである | auto-test |
| AC-61 | 系列選択の既存の安全策が維持される（検索欄に文字を入れただけでは確定しない／種別不一致・存在しない系列 ID はサーバーで拒否／新規作成は明示確認が必須） | auto-test |
| AC-62 | 既存テスト・lint・typecheck が CI で green である | auto-test |
| AC-63 | 本番の `short_name` が `null` の既存2系列（九段／北海道競技かるた初心者大会）に通称が投入されている | manual |
| **D・E 級の地域制限判定（2026-09-08 追加）** | | |
| AC-64 | `record_extraction` の input_schema の各単位に `regional_eligibility` があり、要素は `grade`（D・E のみ）・`verdict`（4値）・`evidence_quote` を持つ。`grade` が D・E 以外、同一単位内で `grade` が重複、`verdict` が4値以外のペイロードは validate に失敗する | auto-test |
| AC-65 | `verdict` が「制限なし」「北海道は対象」「北海道は対象外」なのに `evidence_quote` が `null` または空文字のペイロードは validate に失敗する。「要確認」は `null` を許す | auto-test |
| AC-66 | `PROMPT_VERSION` が `3.1.0` で、生成される `tournament_drafts` 行に記録される | auto-test |
| AC-67 | システムプロンプトに、判定対象が D・E 級であること・4値の定義・「北海道の選手」の定義・優先枠は制限ではないこと・根拠は資料からそのまま抜き出し整形しないことの指示と、D・E 級で条件が異なる few-shot 例が含まれる | auto-test |
| AC-68 | ワーカーが根拠の一文を、AI に渡した本文＋選択添付の抽出テキストと**空白除去＋NFKC 正規化のうえ部分文字列一致**で照合し、結果を判定と一緒にドラフトへ保存する。①一致→照合済み ②言い換え・省略→未照合 ③抽出テキストの無い画像 PDF だけの入力→未照合 ④抽出テキスト側に「202 6 年」のような空白が混じっていても一致、の4ケースで検証する | auto-test |
| AC-69 | 照合結果・判定内容に関わらずドラフトは `pending_review` で作成され、承認ボタンは押せる | auto-test |
| AC-70 | 承認フォームで、照合済みの「北海道は対象外」の級が対象級の初期値から外れ、大会名の合成にも反映される（A〜E の単位で D・E が対象外 → 通称「兵庫」で「兵庫ABC」）。対象級のチェックを戻すと通常どおり送信される | auto-test |
| AC-71 | 外した級ごとに、級名と根拠の一文を含む警告が単位の上に表示される | auto-test |
| AC-72 | 未照合の「北海道は対象外」と「要確認」の級は初期値から外れず、確認を促す表示（根拠の一文。無ければ「資格の記載が見当たりません」）が出る | auto-test |
| AC-73 | 単位の全ての級が外れたとき、その単位の「このイベントを登録する」が既定 OFF になり、その旨の警告が出る。ON に戻せば登録できる | auto-test |
| AC-74 | 「北海道は対象」「制限なし」の級は初期値から外れない。「北海道は対象」は根拠付きの補足が表示され、「制限なし」はフォームに何も出ない | auto-test |
| AC-75 | 登録済み（materialize 済み）の単位と、`regional_eligibility` を持たない旧ドラフト（3.0.x 以前）では、何も外れず警告も出ない（後方互換） | auto-test |
| AC-76 | AI 抽出結果ビューに級ごとの判定・根拠の一文・照合状態が表示される | auto-test |
| AC-77 | 承認 Server Action（`approveDraftUnits`）の入出力契約は変わらない。送信された対象級をそのまま保存し、判定を再適用しない（既存テストが green） | auto-test |
| AC-78 | 既存の抽出 fixture は `regional_eligibility` を追加するだけで、他のフィールドは1バイトも変えない（D・E を含まない単位は空配列） | auto-test |
| AC-79 | 既存テスト・lint・typecheck が CI で green | auto-test |
| AC-80 | ローカル DB の実要綱 5 件で再 AI 抽出し、判定と根拠を目視確認する: ①兵庫（D・E とも対象外・根拠が原文どおり・A〜C は判定なし） ②丸亀（D・E とも対象外→単位が既定 OFF） ③北海道初心者大会（E=北海道は対象） ④横浜 E 級（制限なし。優先枠を制限と誤認しない） ⑤埼玉 D 級（要確認） | manual |

**検証手段の内訳**: auto-test 76件 / verify 1件 / manual 3件（うち 2026-09-08 改修分は auto-test 16件 / manual 1件）

---

## 5. Non-goals（今回やらないこと）

**機能面**
- **訂正版メールの自動検出**（AI 判定・関連ドラフト提案）。人力で確認して手動編集する
- **参加費の自動導出の UI 配線**。級→金額の定数は `packages/shared` にあるが、events への自動反映は別機能（`grade-entry-fee` の続き）で扱う
- pre-filter（`classification='noise'`）の撤去。一覧のフィルタとしては生きているため触らない
- cron の自動 AI 抽出の復活
- 信頼度スコアによる自動承認・`confidence` に代わる品質指標の導入
- XLSX 添付の抽出復活（脆弱性対応で無効のまま）
- 添付プレビュー・ビューアの改修、選択ダイアログでの中身プレビュー（ファイル名・種別・サイズのみ）

**技術面**
- `tournament_drafts.confidence` 列と `superseded_by_draft_id` 列の DROP（書き込みと表示を止めるだけ）
- `events.fee_jpy` 列の DROP（手入力・将来の自動導出のために残す）
- Opus / Fable 系モデルへの移行
- プロバイダ抽象化レイヤ（`LLMExtractor`）のインターフェース変更
- 添付ストレージ肥大（bytea 無期限蓄積）への対処

**通称と系列の相互連動（2026-09-07 追加分）**
- **AI に通称を作らせること**。今回は DB 照合だけで候補を出す。実運用31件の実測でトップ一致52%・候補提示込みで約7割をカバーでき、不足の主因は AI の読み取り能力ではなく「系列マスタに存在しない大会」だったため。運用してみて不足が残るなら別機能として再検討する（§3.2.3 で `short_name_stem` を AI から人力へ戻した判断とも整合する）
- **既存系列の `short_name` を承認フォームから書き換える・null を自動で埋める**こと。書き込みは新規系列作成時のみ
- **年式表記の大会（「九段大会2026-2」等）の開催紐付け**。回次を持たない大会を edition に載せる設計は別途
- **結果取込 flow②（結果ドラフト承認画面）の系列選択 UX**（`tournament-entry-rosters` の Non-goals を引き継ぐ）
- **自動解決スコアリング（`scoreSeries` / `rankSeriesCandidates`）の変更**。照合対象に `short_name` を足すのは検索側だけ
- **系列マスタの管理画面**（名称変更・別名編集・統合／分割）
- 編集距離ベースの曖昧検索

**D・E 級の地域制限判定（2026-09-08 追加分）**
- **C 級以上の判定**。C 級にも地域制限の実例はある（北九州: 福岡県協傘下または福岡県在住の弐段）が、今回は依頼範囲の D・E に限る。対象級はコード上の定数1箇所にまとめ、必要になれば広げられるようにする
- **抽選の優先枠（主催者枠・地元枠）の抽出・表示**。「北海道は優先枠外」の情報は出さない
- **判定の events への保存**、大会詳細・会員向け画面への表示、級別 LINE 配信の直接抑止。対象級から外す運用で間接的に達成する。判定と根拠はドラフトの payload に残る
- **会員個人の在住地・所属会（ゲストの登録会を含む）による個別判定**。会全体として北海道の選手が出られるかだけを見る
- **旧ドラフトの一括再抽出**。必要なら既存の「再 AI 抽出」・`reextract` CLI で個別に行う
- **判定による自動承認・自動却下**。却下への誘導は警告の文言に留め、動線は変えない
- **「北海道」以外の会向けの設定 UI**。ホーム地域は定数1箇所にまとめるが設定画面は作らない
- 根拠の一文の**厳密一致**（空白まで含めた一致）の要求。PDF 抽出テキストの空白ゆれで正しい引用が未照合になるため、空白除去＋NFKC の緩い照合に留める

---

## 6. 技術的制約・契約

### 壊してはいけない既存挙動
- **本文は常に AI へ渡る**（[classifier.ts:227](../../../apps/mail-worker/src/classify/classifier.ts) の `bodyText ?? bodyHtml ?? ''`）。添付選択の導入で本文が落ちる実装にしない
- **`formal_name` は edition（開催）名寄せの種**（[[id]/page.tsx:221-231](../../../apps/web/src/app/(app)/admin/mail-inbox/[id]/page.tsx) → `loadEditionSelectionData`）。`short_name_stem` を消しても `formal_name` の抽出品質は落とさない
- `composeTitle()` と `title.ts` は**残す**（stem の供給元が AI から人間に変わるだけ）
- `LLMExtractor` インターフェースは維持する
- `triggerExtractDraft` の多重起動ガード（`FOR UPDATE` + `triageStatus` / `linkedEventId` / `ai_processing` 検証）と `reextractDraft` の payload 競合対策を維持する
- cron（`--mode=fetch`）が `ANTHROPIC_API_KEY` なしで動くこと

### データ・互換性
- **保存済み `extractedPayload` に Zod を再実行しない**方針を維持する。Web 層は防御的ナローイングで読んでおり（[[id]/page.tsx:103-122](../../../apps/web/src/app/(app)/admin/mail-inbox/[id]/page.tsx)）、スキーマから必須フィールドを消しても既存行は壊れない
- `confidence` / `superseded_by_draft_id` 列は残す。既存行の値も残す
- **`capacity_total` の保存先には `events.capacity` 列が既に存在する**（`capacity_a`〜`e` と併存）。マイグレーション不要
- **`events.payment_deadline_kind` は新規列**（pgEnum `fixed`/`later_notice`/`unspecified`、notNull default `unspecified`）。CHECK 制約と backfill は 3.2.7 のとおり
- 添付選択の永続化には**マイグレーションが必要**（保存先の設計は技術計画で確定する）
- `PROMPT_VERSION` の版数で新旧ペイロードを判別できる状態を保つ

### セキュリティ・権限
- AI 抽出のトリガーは従来どおり `admin` / `vice_admin` のみ
- 添付 ID の選択は**サーバー側で当該メールへの所属を検証**する（他メールの添付を読ませられない）

### 前提として認識しておくこと（今回は変更しないが、影響を理解した上で残す）
- **pre-filter は手動経路の入口を塞ぎうる。** cron は `classification='noise'` を付け、一覧の既定ビューはそれを除外する。`force: true` が prefilter をバイパスするのは **classifier の内部だけ**であり、一覧に出てこないメールは管理者が見つけられないため「会で流す」を押しようがない。本物の大会案内が pre-filter で noise 判定された場合、**一覧の「ノイズ」フィルタから探すのが唯一の救済経路**である
- **`oversize_skipped` ガードは合計サイズ専用になる。** 1件ごとのサイズでは止めないので、この経路に落ちるのは「選択した PDF の合計が 32MB 予算を超えた」ときだけ。**Server Action 直叩き・多重タブ・選択未指定（NULL）の CLI 経路に対する最後の砦であり、削除してはならない**

### 利用技術上の制約
- Anthropic API のリクエスト上限は 32MB。base64 で約 1.33 倍に膨らむため、選択した PDF の**合計**を生バイトで約 23MB に収める（超えたリクエストは 413 `request_too_large` で確実に失敗する）
- prettier 設定はリポジトリに無い。single-quote / セミコロン無しのスタイルを手で維持する

### 通称と系列の相互連動（2026-09-07 追加分）

**壊してはいけない既存挙動**
- **`tournament_series.short_name` は表示側の既存消費者を持つ**（大会一覧（年別）の通称表示 `tournamentDisplayTitle` / 選手戦績の `formatTournamentLabel`）。列の意味・値を変えない
- **自動解決と検索の分離**（`tournament-entry-rosters` PR #292 の設計）。`scoreSeries`（双方向包含・自動解決用）と `searchSeriesCandidates`（一方向・管理者検索用）は別契約であり、`short_name` を照合対象へ足すのは後者だけ
- **系列は ID で確定する**。検索文字列を承認時に再名寄せして既存系列へ解決し直さない（`editionSeriesId` をトランザクション内で存在・種別再検証する現行経路を維持）
- **新規系列作成は明示確認が必須**。通称欄に文字が入っただけでは作成しない
- 個人戦／団体戦の種別不一致の系列は選択させない（混在案内では候補ゼロ）
- 通称が未入力なら大会名は空のまま（AC-17）。自動投入は「候補1件」のときだけで、それ以外は現行どおり空

**データ・互換性**
- **マイグレーションは不要**。`short_name` 列は既存（migration 0038）。新規系列作成時に値を入れる経路が増えるだけ
- 全系列（182件規模）を1回の読み取りでクライアントへ渡す既存契約を維持する。候補チップの絞り込みはクライアント側で行い、**追加のラウンドトリップ・検索 API を作らない**
- `SeriesRow` に `shortName` を足すのは表示と検索のためであり、既存の自動解決呼び出し側の挙動は変えない

**セキュリティ・権限**
- 承認・系列作成の権限は従来どおり `admin` / `vice_admin`

**運用作業（コード外）**
- 本番の `short_name` が `null` の2系列（九段 id=182 / 北海道競技かるた初心者大会 id=181）に通称を投入する（UPDATE 2行）

### 未解決の技術論点（→ 技術計画で解決）
- 添付選択の永続化先（`tournament_drafts` の配列列 / 中間テーブル / `mail_attachments` のフラグ）
- `classifyMail` への選択の受け渡し方（オプション引数 / DB から都度読む）
- `capacity_total` → `events.capacity` の対応が妥当か（既存の `events.capacity` が別用途で使われていないかの確認）
- enum の日本語値を DB にそのまま入れるか、既存の `events.paymentType`（`advance` / `onsite`）へマッピングするか

### D・E 級の地域制限判定（2026-09-08 追加分）

**壊してはいけない既存挙動**
- `LLMExtractor` インターフェースと forced tool use（`record_extraction` 1回）の形は維持する。スキーマ変更は**追加のみ**
- **承認 Server Action の契約を変えない**。対象級を外すのは承認フォームの初期値だけで、サーバー側は送信値をそのまま保存する（`extractEventUnitsFormData` の `grade_*` 読み取りは不変）
- `composeTitle()` は不変。入力に渡す級の集合が「初期値から外した後の集合」になるだけ
- `source_mismatch` の警告バナー・承認をブロックしない原則（§3.2.6）は維持する
- 既存の fixture ベーステスト（`FixtureLLMExtractor`）の回帰ベースラインを壊さない（AC-78）

**データ・互換性**
- **マイグレーション不要**。判定・根拠・照合結果は `tournament_drafts` の抽出 payload（JSON）内に持つ
- 保存済み payload に Zod を再実行しない方針を維持し、Web 層は `regional_eligibility` の有無を防御的に読む（無ければ何もしない）
- `PROMPT_VERSION` を `3.1.0` に上げ、版数で新旧を判別できる状態を保つ

**照合の入力**
- 機械照合の対象は **AI に実際に渡した入力と同一**（本文＋選択した添付の抽出テキスト）。未選択の添付は照合にも使わない
- 正規化は「空白類（全角スペース含む）の除去」と「Unicode NFKC」だけ。それ以上の寛容化（句読点無視・編集距離）はしない

**セキュリティ・権限**
- 変更なし（AI 抽出のトリガー・承認は従来どおり `admin` / `vice_admin`）

**利用技術上の制約**
- ホーム地域「北海道」と判定対象級「D・E」は、プロンプトと UI 文言が共有する**定数1箇所**に置く（他会への配布時に差し替える箇所を1つにする）

**未解決の技術論点（2026-09-08 追加分 → 技術計画で解決）**
- 照合結果の保存形（`regional_eligibility[]` の各要素にワーカーが `evidence_verified` を書き足す／payload の別キーに持つ）と、tool の input_schema にその項目を含めない方法
- 照合に使う抽出テキストの取得点（`classifyMail` が既に手元に持つ入力をそのまま使えるか）
- 承認フォームの警告ブロックの置き場所（単位カード内・登録チェックの直下）と、既存の `source_mismatch` バナーとの部品共有
- 既存 few-shot 例（Example 3 の A〜D 級）への `regional_eligibility` の追記内容

---

## 7. 設計判断の根拠

### なぜ分類フィールドを「消す」のか（残して無視しないのか）
残すと AI は必ず値を埋めようとし、その判断のために資料を読む。人間が既に下した判断を AI に再実行させるのは、トークン代を払って矛盾リスクを買う行為になる。`is_tournament_announcement: false` と `events: [...]` が同時に返る自己矛盾を `superRefine` で弾いている現状の複雑さも、フィールドごと消せば不要になる。

### なぜ `confidence` を再定義せず削除するのか
現行の `confidence` は明確に「**分類**が正しい確率」と定義されている。抽出精度の指標に読み替えても、AI の自己申告値は校正できず、人間が必ず全件レビューする運用では意思決定に使えない。同じものを別名で残すより、`reason` を人間向けの抽出メモに作り替えるほうが実用的である。

### なぜ通称の候補を AI ではなく DB 照合で出すのか（2026-09-07）
ユーザーの当初の要望は「AI が合いそうな通称候補を出す」だったが、本番の直近31ドラフトで実測したところ、**既存の名寄せロジックだけでトップ一致16/31・候補提示込みで約7割**をカバーできた。外れた14件の内訳は (a) 系列マスタに存在しない大会7件（鳳玉×3・熊本地震チャリティー・大学生選手権・全国職域学生・30周年記念）、(b) `short_name` が `null` の新規作成系列3件（九段×2・初心者）、(c) 会の表記と DB の表記のずれ4件（人「椿杯」/DB「椿」、人「東京吉野会」/DB「吉野会」）。**(a) は AI でも候補リストに無いものは出せず、(b) はデータの穴、(c) は正解が一つに定まらない好みの問題**であり、いずれも AI を足して埋まる種類の不足ではない。API コストと抽出待ち時間を払う前に、まず照合だけで運用して不足の実態を見る。

### なぜ通称と系列を「同じ入力」にまとめず、片方向ずつ緩く連動させるのか（2026-09-07）
`tournament_series.short_name` と承認フォームの通称は概念としては同じだが、**実データでは会の表記が DB とずれる**（椿杯/椿・東京吉野会/吉野会。しかも同じ大会で回によって人の打ち方も揺れている）。1つの値に統合すると、系列を選んだ瞬間に会で使いたい表記が消えるか、DB の正準値が会の表記で汚れるかのどちらかになる。そこで**「系列は ID・通称は表示文字列」という別々の値のまま**、空欄を埋める方向にだけ自動を効かせ、既に人が入れた値は上書きしない設計にした。「通称を打ち直しても系列選択は外れない」のも同じ理由（表記を直したいだけで紐付けを壊されると、手間を減らすはずの機能が手間を増やす）。

### なぜ新規系列作成時にだけ `short_name` を書き込むのか（2026-09-07）
系列マスタは180件が投入済みで、穴が空くのは承認フォームから新しく作った系列だけ（実測で九段・北海道競技かるた初心者大会の2件が `null`、その2件が31件中3回出現していた）。**作った瞬間に通称も入れておけば穴は生まれない**。一方、既存系列の `short_name` を承認のたびに書き換えると、大会一覧・選手戦績の表示が承認操作の副作用で変わってしまう。書き込みを新規作成に限れば、既存の表示消費者に一切影響しない。

### なぜ `short_name_stem` を人力入力に戻すのか
通称は「大阪」「札幌」程度の短い地名であり、人間が入力するコストはほぼゼロ。一方 AI にとっては「第N回」「令和N年度」「競技かるた」「選手権」「主催団体名」「級表記」を正しくそぎ落とすという曖昧な判断であり、プロンプトの1節を丸ごと消費していた。合成ロジック（`composeTitle`）は変えず、供給元だけ差し替えるのが最小の変更になる。

### なぜ `fee_jpy` を削除するのか
参加費は級から決定的に導出できる（公認大会の級別参加料は協会規定で A/B 2,500・C/D 2,000・E 1,500、公認料は全級 300）。定数は既に `packages/shared` にある。決定的に求まる値を AI に読み取らせるのは、誤読のリスクだけを増やす。

### なぜ `payment_deadline_kind` を足すのか
現状 `null` が「案内に後日連絡と書いてある」と「AI が読み取れなかった」の両方を意味しており、レビュー時に区別できない。前者は正常な状態で追加調査は不要、後者は人間が原文を当たるべき状態であり、対応がまったく違う。3値の状態フィールドで機械的に判別できるようにする。

### なぜ `payment_method` / `entry_method` を閉じた日本語 enum にするのか
実運用の値がそれぞれ2〜3種類に集中しており（口座振込／現地支払い、Excel 申込書／Google フォーム／メール）、自由テキストのままでは表記ゆれが溜まって将来の集計ができない。`"bank_transfer"` のような英語識別子は UI にそのまま出ると読みにくく、表示側でマッピングを持つより AI に日本語で出させるほうが経路が短い。

### なぜ全体定員と級別定員を両方持つのか
案内の書き方が実際に両方あり、片方に寄せると原文の情報が落ちる。`events.capacity`（全体）と `capacity_a`〜`e`（級別）の列は既に併存しているため、スキーマ側の受け皿も揃っている。片方からの逆算禁止は従来どおり維持する。

### なぜプロンプトキャッシュを撤去するのか
キャッシュは「同一プレフィックスを TTL 内に複数回叩く」ことで元が取れる。書込は 5m TTL で 1.25×、1h TTL で 2.0×、読込は 0.1×。運用が「メールが来たらその都度1件ずつ」に変わったため、どの TTL でもヒットせず書込プレミアムだけを払い続ける。

なお [prompt.ts:31-36](../../../apps/mail-worker/src/classify/prompt.ts) の「cache_control が効く 2048 トークン閾値を超えるために意図的に長くしている」というコメントは、Sonnet 4.6 / Sonnet 5 の最小キャッシュ長が 1024 であるため元から前提が誤っている。キャッシュ撤去によりこの制約は完全に消える。

### なぜ `thinking: disabled` を明示するのか
Sonnet 4.6 は `thinking` 省略＝思考なしだが、Sonnet 5 は省略＝adaptive thinking が ON になる。`max_tokens: 4096` は思考トークンと出力トークンを**合算して**上限をかけるため、明示しないと `record_extraction` の引数が途中で切れる。構造化抽出タスクに思考は不要であり、コストにもならない。

### なぜサイズ超過を「ブロック」ではなく「警告＋確認」にしたのか
初版は「1つでも上限超過 PDF があればメール全体をスキップ」で、超過に気づく手段すら無かった。そこで選択ダイアログで理由付きに示す形へ変えたが、**示すだけでなく送信自体を禁じていた**ため、次は「大きめの要綱 PDF を AI に読ませられない」が実害になった。`MAIL_WORKER_PDF_SIZE_LIMIT_KB` の根拠は AI 利用コストの目安であって送信可能サイズではなく、管理者が中身を見て選んだ PDF を送信不能にする理由にはならない。注意書きと確認ステップに変え、続行できるようにした。物理的に送れない合計サイズ（32MB 予算）だけはブロックのまま残す —— こちらは確認で通しても 413 で必ず失敗するため。

### なぜ新しい機能スラッグを切ったのか
変更が `mail-tournament-import`・`tournament-title-grade-split`・`mail-inbox-mailer` の3書にまたがるため、いずれか1書の中で表現すると残り2書との整合が取れない。リポジトリの既存慣行（`event-list-refinements`・`senseki-stats-refinements`・`invite-register-redesign`）に倣い、delta を独立スラッグに置き、本書が変更後の唯一の正であることを冒頭で宣言する。

### なぜ D・E 級だけを判定するのか（2026-09-08）
地域制限が付くのは初段以下の級（D・E）がほとんどで、A〜C は協会の登録級だけが条件になるのが通例。C 級にも実例はある（北九州）が、依頼の範囲は D・E であり、判定対象を広げるほど「制限なし」のノイズ判定が増える。対象級は定数にして、運用で C 級の見落としが実害になったら広げる。

### なぜ根拠を「資料からのコピペ」に限定し、ワーカーが照合するのか（2026-09-08）
AI の要約や言い換えは、誤読や捏造をもっともらしい文に包んでしまい、管理者が原文で確かめる手がかりを失わせる。一字一句そのままの引用なら、管理者は要綱を検索するだけで裏が取れる。さらに引用を機械照合すれば、「原文に存在しない根拠で級を外す」事故を構造的に排除できる。照合は自動除外の安全弁であって判定の正否を決めるものではないので、未照合でも判定は残し、人が見る状態へ倒すだけにする。

### なぜ対象外の級を初期値から外し、かつ警告するのか（2026-09-08）
管理者は実質1名で、案内の大半が D・E 級に地域制限を持つ以上、毎回チェックを外す手数は小さくない。一方、黙って外すと AI の誤判定で案内が丸ごと消えるリスクを人が検知できない。「外すが、外したことと根拠を必ず見せる」ことで、手数の削減と可視性を両立する。`source_mismatch`（警告のみ）より一歩踏み込むのは、照合済みの引用という裏付けがあるときに限る。

### なぜ「制限なし」と「北海道は対象」を分けるのか（2026-09-08）
前者は資格文に地域の条件が無い状態、後者は地域の条件があって北海道が含まれる状態で、誤読のリスクが違う。後者は支部や隣接県の読み違いで反転しうるため、根拠付きで管理者に見せる。3値に潰すと、この「確認すべき度合い」の差が消える。

### なぜ events へ持ち回さないのか（2026-09-08）
対象外の級を対象級から外せば、申込・級別 LINE 配信・未回答アラートは既存の `eligible_grades` の仕組みに乗って自然に止まる。判定を別に保存して各画面で読む設計は、同じ事実を2箇所に持つことになる。判定と根拠はドラフトの payload に残るので、後から見返すことはできる。

---

## 8. 変更履歴

- 2026-07-29: 初版。運用変更（AI の役割が分類＋抽出→抽出のみに縮小）に伴い、Sonnet 5 移行・分類ロジック撤去・抽出項目の整理・添付選択 UI 新設・PDF サイズ上限引き上げ・本文常時送信の回帰固定を定義（理由: cron の自動 AI 抽出が既に廃止され人間が事前判断する運用になっているのに、プロンプトとスキーマが旧運用のまま取り残されていたため）
- 2026-09-07（実装時の明確化）: AC-47 と §3.2.9(a) の初期選択条件を「候補1件のときだけ採用」から「**完全一致が単独** または **候補が1件**」の2分岐へ明文化した（理由: 既存の `buildEditionSuggestion` は「完全一致が単独」なら他に部分一致があっても採用していた。文字どおり置き換えると緩和のはずが現行の自動化を狭めてしまうため。原文は「現行どおり」と書かれており、狭める意図ではなかったと判断した）
- 2026-09-07: 通称と系列を相互連動させる改修を追加（§3.2.9〜3.2.11 新設・§3.2.3 に参照追記・AC-45〜AC-63 追加）。名寄せ候補が1件なら通称と系列を自動投入し、通称欄への入力が系列候補チップになり、系列選択が通称を埋め、新規系列作成時に通称が `short_name` として保存される。あわせて系列検索の照合対象に `short_name` を追加し、回次パースを漢数字に対応させる（理由: 承認画面で通称と系列にまったく同じ語を二度打っており、系列紐付けが「手間だから」直近31件中7件しか行われていなかった。通称は `tournament_series.short_name` と同じ概念であり、本来1回の入力で両方決まるべきだった）
- 2026-08-29: 1件ごとの PDF サイズ上限を「拒否」から「注意書き＋確認ステップ」へ改訂（AC-27 / AC-32 / AC-35 / AC-36 と §3.2.4・§3.2.5・シナリオ C' を更新）。あわせて合計ガードの予約枠を 6MiB→1MiB に圧縮し、実用上限を約 19.5MB→約 23.25MB へ拡大（理由: 目安サイズを超える要綱 PDF を AI に読ませられず、しきい値の根拠がコストの目安でしかなかったため。予約枠は見積りにすぎず、厚くした分だけ「実際は 32MiB に収まるのに送れない PDF」が増えるうえ、送信直前の実測ガードが最終防衛線として別に配線されている）
- 2026-09-08: D・E 級の地域制限と北海道の出場可否を AI に判定させる改修を追加（§3.2.12 新設・§3.2.2 に `regional_eligibility` 追加・§3.1 に承認フォームの警告ブロック・AC-64〜AC-80 追加・`PROMPT_VERSION` 3.1.0）。4値判定＋資料からそのままの根拠の一文＋ワーカーの機械照合、照合済みの「北海道は対象外」の級を承認フォームの初期値から外して警告表示する（理由: D・E 級の要綱の大半に地域制限があり、管理者が毎回原文を読んで判断していた。見落とすと出場できない大会が会員に案内され、申込グループや級別配信まで作られる）

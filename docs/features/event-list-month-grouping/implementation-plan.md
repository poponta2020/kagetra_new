---
status: completed
---
# 大会申込一覧 月区切り 実装手順書

純UI（新ロジック・新データ皆無）のため design-spec が要件成果物。
正 = [design-spec.md](design-spec.md)（locked）＋ `design-mock/` の2ファイル。

## 実装タスク

### タスク1: 月グループ化とフォーマットの純関数
- [x] 完了
- **目的:** 開催日順の行集合を月ごとに束ねる純関数と、英字月名（AUG等）・英字曜日（SUN等）・
  年跨ぎ判定（年が変わる最初の月だけ西暦を返す）のフォーマッタを用意する
- **対応:** design-spec §2-1, 2-3, 2-6, §6 チェックリスト該当項目
- **主な変更領域:** `apps/web/src/app/(app)/events/event-list-utils.ts`（＋同 `.test.ts`）
- **依存タスク:** なし
- **必要なテスト（テストファースト）:** 月境界・年跨ぎ（12月→1月で最初の1月だけ年が付く）・
  フィルタ後の空月がグループに現れない・曜日/月名の英字マッピング・ゼロ埋め（月2桁/日ゼロなし）
- **完了条件:** 追加テスト green・typecheck 通過（CI で確認）

### タスク2: 画面の productionize（月見出し・日付ブロック・二重符号・フッター行）
- [ ] 完了
- **目的:** design-mock の見た目を実装に移植する。月見出し（T-4）挿入、日付を左ブロックへ、
  大会名の太さ二重符号、ページ見出し行の削除とリスト末尾フッター行への移設
- **対応:** design-spec §2 全項・§6 忠実度チェックリスト
- **主な変更領域:** `apps/web/src/app/(app)/events/EventListClient.tsx`・
  `apps/web/src/app/(app)/events/page.tsx`（見出し行削除・フッター行。新規作成ボタンの
  管理者条件は現行実装を踏襲）・同テストファイル
- **依存タスク:** タスク1
- **必要なテスト:** 開催日順で月見出しが出る／締切日順では出ない・日付ブロックの曜日クラス・
  canApply と太字の連動・フッター行の存在（管理者/一般の出し分け）・既存ソート/フィルタ回帰
- **完了条件:** テスト green・**design-spec §6 忠実度チェックリスト全項目クリア**・
  モックと同じ `--kg-*` トークンのみ使用（新色・新サイズを発明しない）

## 実装順序（Wave）
- Wave 1: タスク1
- Wave 2: タスク2（タスク1 に依存・同ディレクトリのため直列）
